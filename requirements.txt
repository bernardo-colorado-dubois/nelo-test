boto3
python-dotenv
pyspark==3.5.3

# Para que el IDE resuelva los imports de dags/etl_pipeline.py y se pueda
# correr `airflow` localmente (p. ej. `airflow dags list`) sin entrar al
# contenedor. Versión igual a AIRFLOW_VERSION en .env — ver requirements-airflow.txt
# para la imagen de Docker real que efectivamente ejecuta el DAG.
apache-airflow==3.3.1
apache-airflow-providers-apache-spark==6.3.1
